// Auto-Generated
import React from "react";
import cn from "classnames";

import { IconProps } from "./Icon.types";

const Icon: React.FC<IconProps> = ({ children }) => {
	return (
		<i data-testid='Icon' className={cn("icon-class")}>
			{children}
		</i>
	);
};

export default Icon;

// Auto-Generated
import React, { forwardRef } from "react";
import cn from 'classnames';

import { BreadcrumbsProps } from "./Breadcrumbs.types";

const Breadcrumbs = forwardRef<HTMLDivElement, BreadcrumbsProps>(({ children }, ref) => {
    return(
        <div data-testid="Breadcrumbs" className={cn("")} ref={ref}>{children}</div>
    )
});

export default Breadcrumbs;

